{
	"name": "server",
	"version": "1.0.0",
	"main": "src/server.js",
	"license": "MIT",
	"scripts": {
		"lint": "eslint ./src --fix",
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node src/server.js",
		"dev": "nodemon src/server.js"
	},
	"dependencies": {
		"bcrypt": "^5.0.0",
		"cors": "^2.8.5",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"mongoose": "^5.10.2"
	},
	"devDependencies": {
		"eslint": "^7.7.0",
		"nodemon": "^2.0.4"
	}
}
